# Agent Dev Team & Orchestration Playbook

Set up a persistent agent dev team that implements FairShareMonApi feature-by-feature from `The-ideal.md`, with a user checkpoint after every feature.

## Objective

Define reusable role agents (planner, implementer, test engineer, reviewer) plus the orchestration protocol the main Claude Code session runs per feature, so every feature cycle follows the repo's Clarification-First and planning-doc-first rules mechanically.

## Background

- `The-ideal.md` (feature spec) is final; conventions live in `CLAUDE.md` / `AGENTS.md` / `.agents/rules/rules.md`.
- The infrastructure init plan (`planning/project-initialization.md`) is approved and unblocked; code is still the bare webapi template.
- User decisions 2026-07-13: scope = **API first, Web later**; team form = **persistent agents in `.claude/agents/` + orchestration**; cadence = **checkpoint per feature**.

## Requirements

- Four role agents defined at the workspace root `FairShareMon/.claude/agents/` (one level above this repo, so the same team later serves `FairShareMonWeb`):
  - `feature-planner` — drafts `/planning/<feature>.md` (template-compliant), read/plan only, never assumes: unknowns become Open Questions.
  - `api-implementer` — builds strictly per the approved doc; conventions embedded in its prompt; keeps the doc's Progress Log updated.
  - `test-engineer` — owns `FairShareMonApi.Tests`; unit + real-MariaDB integration tests (skippable, rollback-isolated); never fixes production code.
  - `code-reviewer` — read-only audit of the diff against business rules + conventions; blocking vs nit findings.
- Agents cannot ask the user directly → every unknown is written into the planning doc's Open Questions and surfaced at the checkpoint by the orchestrator.
- Git commits are made only by the orchestrator, in this repo.

## Orchestration protocol (per feature)

1. **Plan** — `feature-planner` drafts/updates `/planning/<feature>.md`.
2. **Clarify** — orchestrator brings Open Questions to the user; answers recorded in the doc's Decision Log.
3. **Implement** — `api-implementer` builds per the approved doc (incl. authoring the EF migration; `database update` only after review).
4. **Test** — `test-engineer` adds and runs tests; build + full suite must pass (DB tests may skip only when MariaDB is unreachable).
5. **Review** — `code-reviewer` audits the diff; blocking findings loop back to `api-implementer`; re-review until clean.
6. **Close** — Progress Log + Final Outcome updated; git commit; **user checkpoint** approves before the next feature starts.

## Roadmap (each item = one full cycle + checkpoint)

1. **Infrastructure init** — execute the approved `project-initialization.md` (planning already done → starts at step 3 of the protocol). Needs from user: real MariaDB credentials for `ConnectionStrings:Default`.
2. **Auth** — `users`/`auth_tokens` + first migration, register/login/refresh/logout, BCrypt, opaque-token whitelist (Redis + DB), delete DI stubs, password change revokes all tokens.
3. **Members** — CRUD + soft delete; owner-representative member auto-created on register. **(implementation done 2026-07-14)** — closed the M2 owner-rep backfill obligation and established the shared registration-bootstrap seam (`IRegistrationBootstrapStep` + `IUserRepository.CreateWithBootstrapAsync`) that M4 extends for suggested categories.
4. **Categories + Tags** — CRUD, unique active names, default-category invariant, tag reactivation on name reuse. **(implementation done 2026-07-14)** — reused the M3 `IRegistrationBootstrapStep` seam (`SuggestedCategoriesBootstrapStep` + a mirrored idempotent `SuggestedCategoriesBackfillHostedService`) for suggested-category seeding ("Ăn uống" default + 4 more); shipped the default-category always-exactly-one/not-deletable invariant with an atomic user-scoped swap, and accent/case-insensitive reactivation-on-name-reuse for both categories and tags.
5. **Expenses + Shares + Audit** — atomic expense+shares, share sub-routes, settled flag, filters, immutable audit log. **(implementation done 2026-07-14)** — closed the M4 OQ10 expense-linking deferral (built the expense↔category FK + expense↔tag join table + §4.2/§4.8 cross-user link validation); shipped the atomic expense+shares+audit transaction (one `ExecuteTransactionAsync`, hard-delete cascade with surviving audit), the immutable per-entity snapshot audit log (no-FK refs, no-op suppression, settled unaudited), the derived expense total, owner-rep 0đ auto-inject + protection (7002/7003), and the codebase's first money CHECK constraint (`ck_shares_amount_non_negative`, `decimal(18,2)`). Leaves the event relationship as the M6 seam (no `event_id`; dedicated `SetSettledAsync` for the closed-event exception).
6. **Events** — lifecycle, expense-date-within-event validation, one-way close, closed-event write blocking (settled exception). **(implementation done 2026-07-14)** — consumed the M5 OQ8 event seam (added the nullable `event_id` FK on `expenses`, `OnDelete(SetNull)` so deleting an event leaves its expenses loose); shipped the `events` table (+ the codebase's 2nd CHECK `ck_events_date_range`) and `Event`/`EventRepository`/`EventsService`/`EventProfile`/validators/`EventsController` (6 routes) + the two expense-side assign/remove routes; the **one-way close** lifecycle (re-close → 9001, no reopen, no auto-close job); the **§4.4 closed-event write-blocking** via `EventWriteGuard` on all 7 M5 write paths (ExpenseRepository UpdateGeneralInfo/Delete/AssignEvent/RemoveEvent + ShareRepository Add/Update/Delete), **`SetSettledAsync` the sole exempt path**; **whole-day-inclusive UTC** expense-time-within-range validation (create-into/assign/expense_time-edit → 9002, range-edit-excludes-assigned → 9003); create-into-event + direct A→B move; the `eventUuid`/`looseOnly` filters + expense-DTO event fields; and the 9xxx error block. Event membership is not audited (OQ6). Leaves the M7 balance/stats + M8 export + M9 QR seams (they consume the `events` table + `is_closed`/`closed_at` timeline).
7. **Debt balance + Stats** — per-event balance, overview + per-category stats. **(implementation done 2026-07-14)** — shipped the core per-event debt balance (per-member advanced/owed/balance, **sum-to-zero by construction** — advanced + owed resolved from the same single event-scoped share-set, grouped by `Expense.PayerMemberId` vs `Share.MemberId`; settled-ignored, loose-excluded, deleted-member-inclusive, available for OPEN + CLOSED) plus the overview (`totalSpending` + `expenseCount` over an optional inclusive UTC range) and per-category (total + count + color/icon, time-range XOR event, deleted-category-with-history included, sort total DESC) stats, all via **DB-side `GROUP BY`/`SUM`/`COUNT` aggregation** — establishing the efficient server-side pattern the M5/M6 reviews flagged. New `StatsRepository`/`StatsService`/`StatsProfile`/`Models/Stats`/`Validators/Stats` + `StatsController` (`GET /stats/overview`, `GET /stats/by-category`); `EventsController` gained `GET /events/{uuid}/balance`; reuses `EventNotFound` 9000 + `ValidationFailed` 1001 and reserves the 10xxx block (comment only). **NO schema change / no EF migration** (pure read-aggregation). Leaves the M8 export + M9 wallet/QR (consumes the per-owing-member balance) + M10 tiers seams.
8. **Export CSV** — expense + event exports, format-extensible design. **(implementation done 2026-07-14)** — shipped format-extensible CSV export for a single expense (header block + per-member share table + total row) and a single event (header + per-member share summary + the embedded **M7 debt-balance table**, no per-expense list), behind a neutral string-only `ExportDocument` IR + `IExportFormatter` keyed by the `ExportFormat` enum (`Multiple = true`) with one `CsvExportFormatter`/`CsvWriter` and a centralized `ExportValueFormatter` — so Excel/JSON is a future formatter class only, no controller/service/DTO change. Returns an unwrapped `FileContentResult` (UTF-8 **with BOM**, RFC-4180, CRLF, `text/csv; charset=utf-8`) that bypasses `[ResponseWrapped]` with **no `AppController` edit** (non-`ObjectResult` → filter no-ops; thrown `ErrorException`s still wrap; actions carry `[Produces("text/csv","application/json")]` so the error path never 406s). Local **+7** datetime formatting for instants (`dd/MM/yyyy HH:mm`) vs **no-shift** calendar dates for the event range (`dd/MM/yyyy`) — the OQ6 split, verified live + unit-tested. Reuses the M5/M7 services (derived total, balance rows for both event sections — no recompute), soft-deleted members/categories shown `(đã xóa)` (§4.7), merged notes joined `"; "` prefixed by expense name, diacritic-folded filename slugs. New `GET /expenses/{uuid}/export` + `GET /events/{uuid}/export` (`?format=csv`, default csv; unsupported → 400/1001; resource-owned miss → 6000/9000; open + closed events both export); **11xxx Export** error block reserved (no new codes); no tier gate (CSV is Free). **NO schema change / no EF migration / no new NuGet dependency** (AutoMapper still 13.0.1). Leaves the M9 wallet/QR + M10 tiers seams (M10 may gate the non-CSV formats + harden CSV formula-injection for shared exports).
9. **Wallet + QR** — bank accounts, default account, per-expense QR, per-event composite QR image. **(implementation done 2026-07-14)** — shipped the bank-account **wallet** (`bank_accounts` table + `AddBankAccounts` migration; `BankAccountsController` at `api/v1/bank-accounts` CRUD + `PUT /{uuid}/default`) with the **exactly-one-default invariant** (auto-default the first account, atomic user-scoped set-default swap that is never zero/two, delete-of-default promotes the most-recent remaining, hard-delete no `is_deleted`), mirroring the M4 default-category pattern. **QR standard resolved: VietQR** — a hand-rolled `VietQrPayloadBuilder` (EMVCo TLV + CRC-16/CCITT-FALSE, NAPAS `A000000727`+BIN+account+`QRIBFTTA`) so no VietQR payload library is needed. `GET /expenses/{uuid}/qr` returns a per-expense QR **PNG** (amount = M5 derived total) via **QRCoder** `PngByteQRCode` (+ `?format=payload` raw string); `GET /events/{uuid}/qr` is **closed-only** and returns a per-event **composite QR image** (**SkiaSharp**) with one QR per still-owing member drawn from the **M7 balance** (negative = owing, no recompute). Both reuse the M8 `FileContentResult` bypass (`AppController` untouched). Added the project's **FIRST runtime dependencies — QRCoder 1.6.0 (MIT) + SkiaSharp 2.88.9 (MIT/BSD-3) + SkiaSharp.NativeAssets.Linux.NoDependencies** — plus an **embedded Be Vietnam Pro (SIL-OFL 1.1)** Vietnamese font, all license-clean (AutoMapper still 13.0.1, no split-license trap). New 12xxx error block; no tier gate (a clean seam left for M10 — OQ14); no QR persistence (on-demand). Suite 878/878; code review **APPROVE, 0 blocking**. Leaves only M10 (Tiers).
10. **Tiers (Premium/Free)** — Free-tier **usage-limit enforcement** + **Premium feature-gating** + tier propagation to the principal. Scope (refined 2026-07-14): the `users.tier` column + `UserTiers` enum already exist (M2), so M10 adds — (a) `Tier` on `AuthenticatedUser`/claims (populated on the auth path); (b) create-only, count-based Free limits in `MembersService`/`EventsService` (open events)/`ExpensesService` (per calendar month) CreateAsync, each with a DB-side count repo method, Premium bypassing, breach → a new 13xxx code + Vietnamese message, and the owner-rep bootstrap create path exempt (never affects existing data — §3.11/§4.9); (c) gating the "extended"/Premium-only group — the M9 wallet/QR (the `WalletQrService` tier-gate seam) + non-CSV export formats when added; (d) config-driven limit numbers (`Tiers:Free:` appsettings). **The paid-upgrade path is delivered via M11's manual admin-grant** (self-serve VNPay is a documented future seam). Open (per spec §5, for the M10 clarify step): concrete Free limit numbers; Premium-expiry model (lean indefinite-until-revoked; `premium_expires_at` a future seam); the exact Premium-only feature list.
11. **Admin suite** — **(added 2026-07-14 — split out of M10 as its own milestone)** admin metrics dashboard, manual tier grant/revoke, revenue dashboard, and user administration. **Decisions locked 2026-07-14:** admin privilege = a `users.role` enum (`USER`/`ADMIN`, default `USER`) + a **seeded admin account** from config; an `"Admin"` authorization policy + a new `AdminController : AppController` (`[Authorize(Policy="Admin")]`; non-admins → the already-wired `Forbidden 1004`/403; `AppController` stays LOCKED). Payment = **manual admin-grant only** (no online gateway now — VNPay a future seam): a grant flips `users.tier` → Premium and records the offline **amount + reference (+ note)** in a new append-only **payments/grants table** (`IEntity`, snake_case, money `decimal` + CHECK), which is also the revenue dashboard's data source. Dashboards mirror the **M7 Stats triad** (DB-side `GROUP BY`/`COUNT`/`SUM`, `From`/`To` range) but **cross-user over `users`/`payments` only**. **Critical privacy boundary: admin NEVER sees users' personal ledger data** (members/expenses/events/shares) — admin acts only on account metadata + payments, so §4.1 holds and **no unscoped cross-user ledger queries are built**; revenue shows only upgrade-grant amounts + references. User administration = account-level only (list/get account metadata + grant history; disable/enable, delete, revoke tokens, tier grant/revoke — adds an account-status column). Claims the **13xxx** Admin block. Open (for the M11 clarify step): user-delete semantics (hard cascade vs soft/disable-only); admin password-reset capability; grant record fields + whether revoke records a row; whether the metrics dashboard may show anonymous system-wide ledger counts (lean no); seeded-admin credential handling. See `planning/tiers-premium-free.md` (M10) and `planning/admin-management.md` (M11) once drafted.

## Open Questions

- (Milestone 1, blocking Step 3 of the init plan) Real MariaDB connection credentials — asked at kickoff.
- ~~(Milestone 9) QR standard + image generation approach.~~ **Resolved 2026-07-14: VietQR** (hand-rolled EMVCo TLV + CRC-16 payload — no payload library) + **QRCoder** (per-expense PNG) / **SkiaSharp** (per-event composite PNG) with an embedded OFL Vietnamese font; all license-clean.
- (Milestone 10) Free-tier limit numbers; Premium-expiry model; exact Premium-only feature list — for the M10 clarify step.
- (Milestone 11, added 2026-07-14) User-delete semantics (hard-cascade vs soft/disable); admin password-reset; grant record fields; whether the admin metrics dashboard may show anonymous system-wide ledger counts; seeded-admin credential handling — for the M11 clarify step.
- **Payment mechanism (Milestones 10/11): resolved 2026-07-14** — manual admin-grant only for now (grant records the offline amount + reference; revenue dashboard aggregates grants); self-serve VNPay redirect (portable from the `quick-ordering` sibling) is a documented future seam, not built.

## Assumptions

- Agent definitions live outside this repo (workspace root) and are therefore unversioned until the user decides whether `FairShareMon/` becomes its own repo — flagged, accepted for now.
- `FairShareMonWeb` untouched until the API contract stabilizes; the team gains frontend roles then.

## Impact Analysis

- **APIs/Database/Services:** none by this work item itself — it only creates agent definitions and this doc. All product impact flows through the per-feature cycles.
- **Documentation:** this doc; per-feature docs follow.

## Decision Log

### Decision
Four-role team (planner / implementer / test engineer / reviewer); orchestrator (main session) handles clarification, git, and checkpoints.

### Reason
Maps 1:1 onto the repo's mandatory process (planning doc → clarify → implement → verify) while keeping the pieces that need user interaction and repo-state authority in the main session, since subagents cannot prompt the user.

### Alternatives Considered
- Single do-everything agent per feature — loses the independent review gate.
- Separate doc-keeper agent — folded into planner (pre) and implementer/test-engineer (progress log) to avoid a low-value handoff.

### Decision
Migration apply timing (2026-07-13, resolves the reviewer's Milestone-2 process note): during **Implement**, the api-implementer authors the migration, self-reviews it, and applies it to the **local dev DB** when the orchestrator's assignment says so. The code-reviewer still audits the migration in **Review**; a defect found there produces a corrective migration (or a dev-DB reset) before the milestone closes. For any non-dev environment, applying always waits for Review.

### Reason
Integration/endpoint tests need the schema live during the Test step, which precedes Review in the protocol; the dev DB is disposable, so the cost of a post-review correction is near zero while blocking Test on Review would serialize the whole cycle.

### Alternatives Considered
- Apply only after Review (the auth plan's original wording) — would force Test to run before the tables exist or insert a second review round-trip per feature.

### Decision
Agent files at `FairShareMon/.claude/agents/` (workspace root), not inside this repo.

### Reason
Claude Code loads project agents from the working directory's `.claude/agents/`; sessions run at the workspace root, and the Web app will share the team later.

### Alternatives Considered
- Inside this repo (`FairShareMonApi/.claude/agents/`) — versioned, but not discovered when the session cwd is the workspace root.

## Progress Log

### 2026-07-13

- User approved scope (API first), team form (persistent agents + orchestration), cadence (checkpoint per feature).
- Created `feature-planner`, `api-implementer`, `test-engineer`, `code-reviewer` under `FairShareMon/.claude/agents/`; wrote this playbook.
- Next: Milestone 1 (infrastructure init) via the protocol, starting at Implement since `project-initialization.md` is already approved.
- **Milestone 1 executed end-to-end through the protocol** (implementer Steps 1–4 → test-engineer Step 5 → reviewer: 0 blocking + 1 should-fix + nits → implementer fix round → reviewer delta re-review: APPROVE → orchestrator Step 6 verification). Result: 42 tests (39 pass / 3 DB-skips), live boot verified. First full cycle of the team worked as designed.
- Environment note: MariaDB (3306) and Redis (6379) were both unreachable on this machine during the milestone — boot tolerates it by design; DB integration tests skip until the servers are started.

### 2026-07-14

- **Milestone 4 (Categories + Tags) completed the full protocol cycle:** planner drafted `planning/categories-and-tags.md` with 12 Open Questions → all 12 answered at the user checkpoint (11 at recommended option (a), OQ1 at option (b) — "Ăn uống" default seed set) → api-implementer built Steps 1–8 (entities + `AddCategoriesAndTags` migration applied to the dev DB, 4xxx/5xxx error blocks, repositories/services/validators/DTOs/controllers, the suggested-category bootstrap step on the shared M3 seam + a mirrored idempotent backfill hosted service) → test-engineer added 135 tests (unit + real-MariaDB integration + endpoint), **`dotnet test` 337/337 pass, 0 skipped**, deterministic, DB swept clean → code-reviewer **APPROVE, 0 blocking** (first pass, no fix loop; 4 non-blocking notes accepted).
- **Milestone 3 was also closed earlier this day** (implementation + tests + review APPROVE — see `planning/members.md`), establishing the shared registration-bootstrap seam that M4 reused.
- Committed by the orchestrator to `master` (feature commit follows this doc update).
- **Milestone 5 (Expenses + Shares + Audit) completed the full protocol cycle:** planner drafted `planning/expenses-shares-audit.md` with 20 Open Questions → all 20 answered at the user checkpoint (every recommended option (a); OQ9+OQ10 presented together) → api-implementer built Steps 1–10 (four entities + `AddExpensesSharesAndAudit` migration applied to the dev DB incl. the codebase's first CHECK constraint, 6xxx/7xxx error blocks + 8xxx reserved, three repositories + typed write results, pure `AuditLogFactory` + snapshots, `ExpensesService`/`SharesService`, profiles/DTOs/validators, `ExpensesController` with all ten `api/v1/expenses` routes; 43/43 live smoke) → test-engineer added 150 tests (suite 337 → **487**: unit + real-MariaDB integration + endpoint) which surfaced **2 production bugs** in audit no-op detection (ExpenseTime `DateTimeKind` mismatch; `Share.Amount` decimal-scale mismatch — false-positives violating OQ9) → api-implementer fix-loop (`AuditSnapshotCanonicalizer`: `SpecifyKind(Utc)` + `decimal.Round(v,2)`), **`dotnet test` 487/487 pass, 0 skipped**, deterministic, DB swept clean → code-reviewer **APPROVE, 0 blocking** (2 informational notes + 1 nit accepted). Closes the M4 OQ10 linking deferral; leaves the M6 event seam.
- **Milestone 6 (Events) completed the full protocol cycle:** planner drafted `planning/events.md` with 16 Open Questions → all 16 answered at the user checkpoint (every recommended option (a)) → api-implementer built Steps 1–9 + 11 (new `Event` entity + `AddEvents` migration applied to the dev DB incl. the codebase's 2nd CHECK `ck_events_date_range` + the `expenses` ALTER adding `event_id` FK SetNull; 9xxx error block; `EventRepository`/`EventWriteGuard`/`EventsService`/`EventProfile`/validators/DTOs/`EventsController` 6 routes + the two expense-side assign/remove routes; the closed-event guard woven into all 7 M5 write paths with `SetSettledAsync` exempt; within-range validation; create-into-event + move; `eventUuid`/`looseOnly` filters + expense-DTO event fields; 39/39 live smoke; one bug self-found+fixed in smoke — `ExpensesService.DeleteAsync` now routes through `ThrowIfFailed` so a closed-event delete returns 9001 not 6000) → test-engineer added the full M6 suite (unit + real-MariaDB integration + endpoint), **`dotnet test` 606/606 pass, 0 skipped**, deterministic, DB swept clean, **0 production bugs found** → code-reviewer **APPROVE, 0 blocking** (Nit 1 — trim `EventUuid` on create-into-event — fixed by the implementer; Nit 2 — `ListByUserAsync` count-via-`Include` — recorded as a Future Improvement; 1 informational note on the intentional redundant `(user_id)` index). Consumes the M5 OQ8 event seam; leaves the M7 balance/stats + M8 export + M9 QR seams. Committed by the orchestrator.
- **Milestone 7 (Debt balance + Stats) completed the full protocol cycle:** planner drafted `planning/debt-balance-and-stats.md` with 15 Open Questions → all 15 answered at the user checkpoint (every recommended option (a)) → api-implementer built Steps 1–6 with **NO EF migration / no schema change** (pure read-aggregation over the existing M5/M6 tables): `Repositories/Stats/StatsAggregates.cs`, `IStatsRepository`/`StatsRepository` (DB-side `GROUP BY`/`SUM`/`COUNT`), `IStatsService`/`StatsService`, `Models/Stats/*`, `Validators/Stats/*`, `Mappings/StatsProfile.cs`, new `StatsController` (`GET /stats/overview`, `GET /stats/by-category`), `EventsController` gained `GET /events/{uuid}/balance`, `ErrorCodes.cs` reserves the 10xxx block (comment only, reuses 9000 + 1001); 35/35 live smoke → test-engineer added 58 tests (unit + real-MariaDB integration + endpoint; suite 606 → **664**), **0 production bugs found, production code untouched** → code-reviewer **APPROVE, 0 blocking** (3 benign informational notes: deterministic balance-row ordering; display joins keyed by `Id` safe because ids come only from the user's own scoped rows; event-mode vs time-mode DTO echo per the OQ8 contract). **`dotnet test` 664/664 pass, 0 failed, 0 skipped**, deterministic (re-run identical), DB swept clean. Verified: sum-to-zero correct by construction (one event-scoped `shares` IQueryable grouped by payer for advanced + by member for owed → Σadvanced==Σowed==Σshares → balances sum to exactly 0m; no second scope feeds advanced, no tag join inflates), DB-side aggregation with no client-eval blowup, settled-neutral, loose-vs-event scoping correct, deleted inclusion via `includeDeleted:true`, resource-owned + per-user isolation with both-scope rejection validated before the ownership resolve. Establishes the efficient server-side aggregation pattern the M5/M6 reviews flagged; leaves the M8 export + M9 wallet/QR (consumes the per-owing-member balance) + M10 tiers seams. Committed by the orchestrator.
- **Milestone 8 (Export CSV) completed the full protocol cycle:** planner drafted `planning/export-csv.md` with 20 Open Questions → all 20 answered at the user checkpoint (**19 at recommended option (a); OQ6 at option (b)** — local Vietnamese `dd/MM/yyyy HH:mm` / `dd/MM/yyyy` dates instead of ISO-UTC) → api-implementer built the milestone with **NO EF migration / no schema change / no new NuGet dependency** (hand-rolled RFC-4180 writer): `Models/Export/*` (`ExportFormat`, the neutral `ExportDocument` IR, `ExportedFile`), `Services/Api/Export/ExportValueFormatter.cs` (centralized OQ5/OQ6 stringifiers — `FormatMoney` invariant `0.00`, `FormatInstant` fixed +7, `FormatCalendarDate` no-shift), `IExportFormatter` + `CsvWriter` + `CsvExportFormatter` (`Multiple = true`, UTF-8 BOM), `IExportService`/`ExportService` (reuses `IExpensesService`/`IStatsService`/`IEventsService`), `ExpensesController`/`EventsController` each gained `GET {uuid}/export?format=csv` returning an unwrapped `FileContentResult`, `ErrorCodes.cs` reserves the 11xxx block (comment only, reuses 6000/9000 + 1001); `AppController` untouched; live smoke incl. the OQ6 date split, the BOM, the `[ResponseWrapped]` bypass, resource-owned 404s, closed-event export → test-engineer added **60 tests** (unit + real-MariaDB integration + endpoint; suite 664 → **724**), **0 production bugs found, production code untouched**, deterministic (run twice), DB swept clean → code-reviewer **APPROVE, 0 blocking** (5 informational notes: CSV formula-injection hardening for shared exports + `GatherMergedNotesAsync` N+1 → recorded as Future Improvements; the `[Produces("text/csv","application/json")]` error-path refinement → Decision Log note; two design confirmations). **`dotnet test` 724/724 pass, 0 failed, 0 skipped.** Verified: OQ6 instant-vs-calendar-date split correct (fixed +7 via `TimeSpan.FromHours(7)`, no DST/`TimeZoneInfo`/double-convert; `end_date` 23:59:59.999999Z keeps its own day), the `FileContentResult` bypass, format-extensibility (string-only IR + enum-keyed formatter + resolver throwing 1001 before any fetch), RFC-4180 + BOM, M7 reuse for both event sections (no recompute), content correctness (§4.7 `(đã xóa)`, merged notes, total row, sum-to-zero balance, safe filename slug). Leaves the M9 wallet/QR + M10 tiers seams (M10 may gate non-CSV formats + harden CSV-injection for shared exports). Committed by the orchestrator.
- **Post-M8 security chore (CSV-injection hardening) completed the full protocol cycle between M8 and M9** (a follow-up chore, not a numbered milestone): planner drafted `planning/csv-injection-hardening.md` with 4 Open Questions → all 4 answered at the user checkpoint (**every one at recommended option (a)** — numeric-safe guard in `CsvWriter.EscapeField`; single-quote `'` prefix; text-only scope; CSV-path-only) → api-implementer hardened the single file `Services/Api/Export/Formatters/CsvWriter.cs` (a `NeutralizeFormula` step run before the RFC-4180 quoting; `=`/`@`/TAB/CR always guarded, `+`/`-` guarded only when the field is NOT a parseable invariant decimal so raw money incl. negatives like `-500000.00` is preserved per M8 OQ5) with **NO migration / schema change / new NuGet dependency / new error code / new message key** → test-engineer added **37 tests** (suite 724 → **761**), **0 production bugs found, production code untouched**, deterministic, DB swept clean → code-reviewer **APPROVE, 0 blocking** (2 informational notes, both documented accepted trade-offs: the bare-number heuristic gap; first-char-only OWASP coverage). **`dotnet test` 761/761 pass, 0 failed, 0 skipped.** Committed by the orchestrator. See `planning/csv-injection-hardening.md`.
- **Milestone 9 (Wallet + QR) completed the full protocol cycle:** planner drafted `planning/wallet-and-qr.md` with **17 Open Questions** → all 17 answered at the user checkpoint (**16 at recommended option (a); OQ3 at option (b)** — a SkiaSharp raster **PNG** event composite over the no-dependency SVG, for universal chat-shareability), resolving the roadmap's long-standing "QR standard + image library" open item (**VietQR** + **QRCoder/SkiaSharp**) → api-implementer built the milestone: `BankAccount` entity + **`AddBankAccounts`** migration applied to the dev DB (one `bank_accounts` table, hard-delete/no `is_deleted`), the 12xxx error block, `BankAccountRepository`/`BankAccountsService` with the race-free exactly-one-default invariant, the hand-rolled `VietQrPayloadBuilder` (EMVCo TLV + CRC-16/CCITT-FALSE), `QrImageService` (QRCoder single PNG + SkiaSharp composite PNG), `WalletQrService` (reuses M5 total + M7 balance, no recompute), DTOs/validators/`BankAccountProfile`, `BankAccountsController` at `api/v1/bank-accounts` + the two QR routes on `ExpensesController`/`EventsController` (M8 `FileContentResult` bypass, `AppController` untouched) — adding the project's **FIRST runtime dependencies (QRCoder 1.6.0 MIT + SkiaSharp 2.88.9 MIT/BSD-3 + SkiaSharp.NativeAssets.Linux.NoDependencies) plus an embedded Be Vietnam Pro (SIL-OFL 1.1) font**, all license-clean → test-engineer added **117 tests** (suite 761 → **878**: unit incl. the VietQR TLV + CRC-16 correctness centre-piece, real-MariaDB integration for the default-account invariant, endpoint incl. the PNG-magic image bypass), **0 production bugs found, production code untouched**, deterministic (two identical full runs), DB swept clean → code-reviewer **APPROVE, 0 blocking** (2 informational notes: destination-account resolution precedes the resource check but is a benign ordering quirk, not a §4.1 leak; SkiaSharp font-stream lifetime safe at 2.88.9 — revisit `RenderComposite` if bumped to 3.x). **`dotnet test` 878/878 pass, 0 failed, 0 skipped.** No tier gate (a clean seam left for M10 — OQ14); no QR persistence. Leaves only **M10 (Tiers)**. Committed by the orchestrator. See `planning/wallet-and-qr.md`.
- **Roadmap scope expanded (2026-07-14) — admin management added; M10 split into M10 + M11 (planning only, no build started):** at the post-M9 checkpoint the user added an admin-management suite and, after a clarify round, chose to **split** it from the Tiers work. Decisions locked: (1) **M10 = Tiers** (Free-tier limit enforcement + Premium feature-gating + tier→principal) and a new **M11 = Admin suite**; (2) admin privilege = a `users.role` enum + a config-seeded admin account; (3) **payment = manual admin-grant only** for now (grant records the offline amount + reference; revenue dashboard aggregates grants), self-serve VNPay a documented future seam; (4) admin scope = metrics dashboard + manual tier grant/revoke + revenue dashboard + user administration, with the **hard privacy boundary that admin never sees users' personal ledger data** (account metadata + payments only — so no unscoped cross-user ledger queries are built; §4.1 preserved). Roadmap items 10 (refined) and 11 (new) updated above; the harness plan is `C:\Users\GHM_MinhKakart\.claude\plans\elegant-stargazing-thunder.md`. **No feature-planner / no code run for M10 or M11 yet — awaiting the user's go per the per-feature protocol.**

## Final Outcome

Team operational. Four role agents live in `FairShareMon/.claude/agents/` (loaded from the next session onward; this session ran the same role prompts on general-purpose agents). Milestone 1 (infrastructure) completed through the full protocol and committed. Checkpoint questions for the user: OQ4 `error.fields` envelope placement; optional rules.md `string? Uuid` → `string Uuid` one-word sync; start MariaDB/Redis to un-skip DB tests.

## Future Improvements

- Frontend roles (web-implementer, ui-reviewer) once `FairShareMonWeb` work starts.
- Consider making `FairShareMon/` a git repo (or moving agent files into a versioned location) so the team definitions are tracked.
