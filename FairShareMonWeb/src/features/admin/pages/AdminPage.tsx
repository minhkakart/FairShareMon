import { StubPage } from "@/routes/StubPage";

/** Admin area stub (guarded by AdminRoute). Built out in the admin cycle. */
export function AdminPage() {
  return <StubPage titleKey="common:nav.admin" />;
}
