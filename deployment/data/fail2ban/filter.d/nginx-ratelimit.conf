# Request floods: any request nginx rate-limited (HTTP 429). Repeated 429s from
# one IP = sustained abuse past the edge limit.
[Definition]
failregex = ^<HOST> .*"[A-Z]+ [^"]+ HTTP/[^"]+" 429
ignoreregex =
