# Account-creation attempts: any POST /api/v1/auth/register (success or failure),
# so the jail caps total registrations per IP per hour.
[Definition]
failregex = ^<HOST> .*"POST /api/v1/auth/register HTTP/[^"]+" \d{3}
ignoreregex =
